// Production configuration for UnityClubXtra

module.exports = {
    // Add production-specific configuration settings, such as database connection strings, etc.
    // Ensure that sensitive information like API keys and secrets are stored securely and not hard-coded here
};
